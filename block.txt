| Backend API + tests | `python -m pytest` (55 tests) âœ…, control scripts operational after `sys.path` fix. | High | Monitor `uvicorn` install paths in orchestrator logs; consider bundling backend venv bootstrap into LabControl start.
| Frontend build + tooling | `npm run check`, `npm run build`, `npm run test`, `npm run storybook:build` âœ… after Vite 7 + happy-dom 20 bump. | High | Add Storybook smoke to release checklist; schedule Vite/esbuild optional dependency cleanup for Linux hosts still hitting rollup native warnings.
| Control Center scripts | `python scripts/control_center.py status` âœ… (snapshot saved under `.labctl/state/aggregate.json`, logs still show missing `uvicorn`/`jupyter` on prior hosts). | Medium | Provide friendlier error when orchestrator sees missing uvicorn/Jupyter; maybe stash recommended install commands in output/README troubleshooting.
| Workshop / DataLab parity | Papermill CLI âœ… â€” `control_center_relay-cli-20251117-052129.ipynb` + `control_center_snapshot.json` emitted under `workshop/notebooks/_papermill/` (archived copies remain in `legacy/datalab`). Workshop shims still backed by pytest. | Medium-High | Reference the snapshot JSON when triaging Ops signals; ensure `workshop/requirements.txt` stays in sync with datalab shim and schedule next run after manifest changes.
