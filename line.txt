| Workshop parity | Papermill CLI ✅ — `control_center_relay-cli-20251117-052129.ipynb` + `control_center_snapshot.json` emitted under `workshop/notebooks/_papermill/` (archived copies remain in `legacy/archives`). Workshop shims still backed by pytest. | Medium-High | Reference the snapshot JSON when triaging Ops signals; ensure `workshop/requirements.txt` stays in sync with the active helpers and schedule next run after manifest changes.