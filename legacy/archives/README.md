# Archived notebooks

This directory mirrors archived notebooks. All active work now lives under `kitchen/notebooks/` so Kitchen remains the single source of truth for recipes, telemetry explorers, and Papermill entry points.

Use `legacy/archives/notebooks/` when you need to inspect a frozen copy of an earlier run. New edits should target `kitchen/notebooks/` instead.
